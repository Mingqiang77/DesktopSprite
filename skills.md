你现在要开发一个Windows桌面悬浮桌面宠物，完整Python项目，使用PyQt5实现。
需求清单：
1. 窗口无边框、置顶显示，始终浮在所有窗口最上层；背景透明，只渲染宠物形象。
2. 支持鼠标拖拽移动宠物位置；鼠标左键点击宠物触发随机表情动画。
3. 宠物内置多套状态：发呆、开心、困倦、点击被戳的反应，状态自动轮换。
4. 右键弹出菜单：退出程序、隐藏宠物、重置位置。
5. 不需要外部图片资源，全部使用代码绘制矢量卡通小动物（小猫咪），不要读取本地图片文件。
6. 程序可以直接单文件运行，依赖写明requirements，代码注释清晰。
7. 不要控制台黑框，打包运行时隐藏cmd窗口。
8. 图片在D:\iKun\iKun\DesktopSprite\images这个下面的ikun.jpg
输出完整可直接运行的py代码，附带简单使用说明。
