你是一名资深Python桌面应用开发工程师，请帮我开发一个Windows桌面悬浮宠物程序。

## 技术要求
- 语言：Python 3.x
- UI框架：PyQt5
- 全局键盘监听：pynput
- 单文件运行，文件名：desktop_pet.py
- 依赖清单单独列出（requirements.txt格式）
- 键盘监听在独立线程运行，不阻塞UI主线程

## 功能需求

### 窗口设置
- 无边框、透明背景，只渲染宠物本体
- 始终置顶显示（所有窗口最上层）
- 启动默认位置：屏幕右下角

### 宠物形象
- 读取本地图片：D:\iKun\iKun\DesktopSprite\images\ikun.jpg
- 图片需做透明处理，去除白色背景（如有）
- 宠物大小：150x150px，支持保持比例缩放

### 交互功能
- 鼠标左键拖拽：移动宠物位置
- 鼠标左键单击：触发被戳反应（严格区分拖拽和点击）
- 鼠标右键：弹出菜单，包含：
  - 隐藏宠物（托盘图标保留，可再次显示）
  - 重置位置（回到屏幕右下角）
  - 退出程序

### 宠物状态系统
内置5种状态，自动轮换（每隔10-20秒随机切换）：
1. 发呆：默认静止，轻微呼吸动效
2. 开心：上下弹跳动画
3. 困倦：左右摇晃 + 眼睛半闭效果
4. 被戳：鼠标点击触发，短暂震动后恢复，同时显示"你干嘛~"气泡
5. 打字中：键盘输入时触发

状态切换有过渡动画，不突兀。

### 键盘联动功能
- 使用 pynput 监听全局键盘输入
- 用户打字时宠物切换为打字状态：
  - 双手/爪子上下交替敲击动画
  - 头部轻微左右晃动
  - 眼睛变为专注表情
- 停止打字超过2秒，自动恢复发呆状态
- 连续打字超过30秒，触发疲劳提示：
  - 伸懒腰动画（身体拉伸效果）
  - 显示轮播气泡文案（见下方规则）

### 气泡提示规则
用以下常量定义，方便自行修改：

# 疲劳提示文案，固定顺序轮播
BUBBLE_TEXTS = ["唱🎤", "跳💃", "Rap🎶", "打🏀"]
# 被戳专属文案
BUBBLE_POKE_TEXT = "你干嘛~"
# 全局轮播索引
bubble_index = 0

触发规则：
- 场景一（打字疲劳）：按顺序取 BUBBLE_TEXTS[bubble_index % len(BUBBLE_TEXTS)]，
  每次触发后 bubble_index += 1，实现循环轮播
- 场景二（鼠标点击被戳）：固定显示 BUBBLE_POKE_TEXT，不影响轮播索引
- 两种场景互不干扰，独立触发

气泡通用样式：
- 显示在宠物头顶正上方
- 圆角矩形、浅色背景、小字体、emoji正常显示
- 淡入淡出动效，显示3秒后自动消失
- 同一时间只显示一个气泡，新气泡出现时旧气泡立即关闭

### 系统托盘
- 程序启动后在系统托盘显示图标
- 托盘右键菜单：显示/隐藏宠物、退出

### 其他要求
- 运行时不显示控制台黑框
- 每个函数必须有注释，关键逻辑说明清楚
- 图片加载失败时显示默认纯色方块占位，不崩溃
- pynput监听失败时给出提示但程序不崩溃

## 输出格式
1. 完整可运行的 desktop_pet.py 代码
2. requirements.txt 内容
3. 简要使用说明（启动方式、打包命令）
4. 注意事项（常见问题 + pynput权限说明）